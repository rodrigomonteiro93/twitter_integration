{
  "name": "Project",
  "scripts": {
    "create-server": "cd ./server && npm install && npx sequelize db:migrate && npx sequelize db:seed:all && npm run dev",
    "create-client": "cd ./client && npm install && npm run dev"
  }
}
