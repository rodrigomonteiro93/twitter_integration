{
  "name": "API",
  "scripts": {
    "create-api": "cd ./api && npm install && npx sequelize db:migrate && npx sequelize db:seed:all && npm run dev",
    "create-frontend": "cd ./frontend && npm install && npm run dev"
  }
}
